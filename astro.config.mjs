import { defineConfig } from 'astro/config';

export default defineConfig({
  // The public URL of the finished site. Update this when you pick a domain —
  // Astro uses it to build absolute URLs for things like social share cards.
  site: 'https://example.org',
});
